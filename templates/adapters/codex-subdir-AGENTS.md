# Subdirectory Instructions

Purpose: Optional nested Codex/AGENTS.md adapter for a specialized project area.
Read when: Working inside this subdirectory.
Skip when: The root `AGENTS.md` already contains all needed rules.

## Scope

This file applies only to this directory and its children.

## Overrides

- Commands:
- Verifier:
- Safety notes:
- Files not to edit:

Keep this file short. Put task state in the project Core Pack, not in nested instruction files.
